import React from 'react';

export default class Sudoku extends React.Component {
  constructor(...props) {
    super(...props);
    this.state = {};
  }

  render() {
    return (
      <div>Test</div>
    )
  }
};
